defmodule TellerSandboxWeb.TransactionController do
  use TellerSandboxWeb, :controller

  # The transactions endpoint should return a list of transactions going back 90 days. There should be 1 transaction per day.

  def all(conn, params) do
  end
end
